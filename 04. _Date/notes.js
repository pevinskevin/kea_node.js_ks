// prefix increment

const currentId = 0;

console.log(++currentId);

// postfix increment

console.log(currentId++);

// UTC
console.log(new Date());

// Local date time
console.log(Date());

// Unix time / Epoch (Seconds since 1970 Jan. 1st)
console.log(Date.now());

